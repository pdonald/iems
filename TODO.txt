PMS
* properties pane
* add/move/delete/connect
* * can add a component
* * move into group
* * move out of group
* * can connect two components (also in a group)
* json output
* makefile output

done
* layout
* * can move a component
* * move within group only
* * can select a component and delete it
* * can select a connector and delete it
* * can select a group and delete it

[preprocess]
tokenize
lowercase
filter

[align]
fast align
fast align rev
sym

sacompile
extract dev
extract devtest

[lm]
kenlm
build_binary

cdec.ini
tune

MTM 2015
* var mainīt Properties
* salikt visus moses procesus
* precompilēt visu, lai var atvērt html failu
* podziņa copy makefile
